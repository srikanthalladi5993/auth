export default function ReferenceOffer() {
  return (
    <div className="page-content">
      <div className="page-section" style={{ padding: '32px 24px' }}>
        <h2>Reference Offer</h2>
        <p>This is the placeholder offer used to validate the Shell + Offer registry + route loading flow.</p>
        <p>
          The Shell owns the lifecycle, the registry owns the offer metadata, and this offer owns its own feature
          rendering.
        </p>
      </div>
    </div>
  )
}
